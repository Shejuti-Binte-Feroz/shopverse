version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: shejuti
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: usersdb
    ports:
      - "5432:5432"
